repos:
  - repo: local
    hooks:
      - id: dev-hygiene
        name: Repository secret and privacy policy
        language: system
        entry: dev hygiene scan --scope staged
        pass_filenames: false
        always_run: true
        require_serial: true
        verbose: true
        stages: [pre-commit]
